@model PraiseProvisions.Models.UserProfile

@{
    ViewData["Title"] = "Create";
}

<h4 class="center">My Profile</h4>
<div class="row mainProf">
    <div class="col-md-4">
        <form method="post" asp-controller="UserProfiles" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="fullName" class="control-label labelName"></label>
                <input asp-for="fullName" class="form-control profBar" />
                <span asp-validation-for="fullName" class="text-danger"></span>
            </div>
            <div class="form-group center">
                <input type="submit" value="Create Profile" class="btn btn-default widthBtn" />
            </div>
        </form>
    </div>
</div>

<div>
    <h5><a href="@Url.Action("Index", "Home")">SEARCH</a></h5>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
